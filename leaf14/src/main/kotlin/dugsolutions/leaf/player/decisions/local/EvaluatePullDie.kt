package dugsolutions.leaf.player.decisions.local

import dugsolutions.leaf.player.Player
import dugsolutions.leaf.random.die.Die

class EvaluatePullDie {

    operator fun invoke(player: Player): Die? {
        return null
    }
}
