package dugsolutions.leaf.chronicle.di

import dugsolutions.leaf.chronicle.domain.PlayerUnderTest

// Default implementation (for chronicle module)
class DefaultPlayerUnderTest : PlayerUnderTest {
    override val playerId: Int = 0
}
